name: "Verify"

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - edited
  workflow_call:
    inputs:
      runs-on:
        type: string
        required: false 
        default: 'ubuntu-latest'

# https://docs.github.com/en/actions/writing-workflows/choosing-what-your-workflow-does/controlling-permissions-for-github_token#defining-access-for-the-github_token-permissions
permissions:
  pull-requests: write
  statuses: write
  # Default
  actions: read
  attestations: read
  checks: read
  contents: read
  deployments: read
  discussions: read
  id-token: none
  issues: read
  packages: read
  pages: read
  repository-projects: read
  security-events: read

jobs:
  semantic_pr:
    name: "Semantic PR"
    runs-on: ${{ inputs.runs-on }}
    steps:
      - uses: actions/checkout@v4

      - name: Install prerequisites
        run: sudo apt update && sudo apt install -y --no-install-recommends git unzip curl make
        
      - uses: ./.github/actions/semantic_pr
        if: github.repository == 'Inetum-Poland/.github'
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - uses: Inetum-Poland/.github/.github/actions/semantic_pr@main
        if: github.repository != 'Inetum-Poland/.github'
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
